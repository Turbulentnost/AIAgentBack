"""Агент сотрудника службы безопасности (security_officer_agent), контур №3.

Контрольный ролевой агент: идентификация контрагента → сбор сведений (1С + внешние
реестры + история + связанность) → сопоставление с критериями → оценка риска →
проект заключения (согласование / условия / отказ) → HITL → фиксация вердикта.
Уровень автономности У1. Архитектура (скелет): внешние обращения — заглушки platform_sdk.
"""

from pathlib import Path

AGENT_ID = "security_officer_agent"
AGENT_DIR = Path(__file__).resolve().parent

__all__ = ["AGENT_ID", "AGENT_DIR"]

from __future__ import annotations

from pydantic import BaseModel

from app.tools.base import StubTool
from app.tools.registry import register_tool


class DocumentToolInput(BaseModel):
    document_id: str | None = None
    task_id: str | None = None


def _register_stub(name: str, description: str, agent_description: str) -> None:
    register_tool(
        StubTool(
            name=name,
            description=description,
            agent_description=agent_description,
            input_model=DocumentToolInput,
            required_permissions=[name],
        )
    )


_register_stub(
    "get_document_text",
    "Возвращает извлеченный текст доступного документа.",
    "Инструмент get_document_text возвращает извлеченный текст документа, загруженного в задачу. Используй его, "
    "когда нужно проанализировать файл пользователя: ОЛ, ТЗ, договор, спецификацию, КД/ТД, XLSX или PDF. "
    "Инструмент возвращает только документы, доступные текущей задаче и пользователю.",
)
_register_stub(
    "list_task_documents",
    "Возвращает список документов, прикрепленных к задаче агента.",
    "Инструмент list_task_documents возвращает список документов, прикрепленных к задаче агента. Используй его "
    "в начале выполнения, чтобы определить, какие документы доступны для анализа, какие типы файлов загружены "
    "и какие документы требуют обработки.",
)
_register_stub(
    "extract_document_tables",
    "Возвращает таблицы, извлеченные из доступного документа.",
    "Инструмент extract_document_tables возвращает таблицы, извлеченные из документа. Используй его, если задача "
    "связана со спецификациями, перечнями оборудования, опросными листами, коммерческими таблицами, закупочными "
    "позициями или табличными техническими параметрами. Инструмент сохраняет заголовки колонок, строки, листы "
    "Excel и диапазоны ячеек.",
)
_register_stub(
    "get_document_metadata",
    "Возвращает технические сведения и статус обработки документа.",
    "Инструмент get_document_metadata возвращает технические сведения о документе: имя файла, тип, размер, статус "
    "обработки, количество страниц, листов, наличие OCR, дату загрузки и принадлежность к задаче. Используй его, "
    "если нужно проверить готовность документа к анализу или понять, почему текст не был извлечен.",
)
_register_stub(
    "get_document_structure",
    "Возвращает структурное представление документа: разделы, пункты, таблицы и приложения.",
    "Инструмент get_document_structure используй для поиска точного места изменения в нормативном документе. "
    "Если структуры нет, агент должен перейти к поиску по chunks и запросить ручной выбор при неоднозначности.",
)
_register_stub(
    "find_document_by_code",
    "Ищет документ по обозначению СТО/И/РГ/ПЛ/ДИ/РИ/ПП.",
    "Инструмент find_document_by_code используй, когда в тексте изменения явно указан код документа. "
    "Возвращай только документы, доступные текущему пользователю через базу знаний.",
)
_register_stub(
    "find_related_documents",
    "Ищет документы, связанные нормативными ссылками или упоминаниями.",
    "Инструмент find_related_documents используй после выбора целевого документа, чтобы сформировать список "
    "документов, которые могут потребовать проверки из-за изменения ссылки или требования.",
)
_register_stub(
    "get_document_sections",
    "Возвращает текст конкретного раздела, пункта или подпункта документа.",
    "Инструмент get_document_sections используй после определения номера раздела/пункта для извлечения текущей "
    "редакции изменяемого места.",
)
_register_stub(
    "check_document_processing_status",
    "Проверяет готовность документа к анализу агентом.",
    "Инструмент check_document_processing_status проверяет, обработан ли документ: загружен ли файл, извлечен ли "
    "текст, созданы ли chunks, выполнена ли индексация в Qdrant. Используй его перед анализом документа, чтобы "
    "не делать выводы по необработанному или поврежденному файлу.",
)

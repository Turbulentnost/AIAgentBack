============================================================
  Доступ к UI: Входящая корреспонденция (agent_nd_front)
  Сгенерировано: {{GENERATED_AT}}
============================================================

ВАЖНО: автоматический вход по ссылке НЕ поддерживается.
Токен хранится в localStorage браузера после ручного входа.
Прямой переход на /agents или /agents/incoming-mail без
авторизации перенаправит на страницу входа.

--- Сеть ---
LAN IP (устройства в той же сети):     {{LAN_IP}}
{{PUBLIC_IP_LINE}}
Порт фронтенда (Vite):                   {{FRONTEND_PORT}}
Бэкенд платформы (API):                  http://{{API_HOST}}

--- Учётные данные платформы ---
Email / логин:  {{EMAIL}}
Пароль:         {{PASSWORD}}

--- URL для LAN ({{LAN_IP}}) ---
Страница входа:                 {{LAN_BASE}}/login
Каталог агентов (после входа):  {{LAN_BASE}}/agents
Входящая корреспонденция:       {{LAN_BASE}}/agents/incoming-mail

{{PUBLIC_SECTION}}

--- Порядок действий ---
1. Запустите API agent-pochta:  python scripts\run_api.py
2. Запустите UI:                run_frontend.cmd
3. Откройте URL «Страница входа» (LAN или внешний)
4. Введите email и пароль из раздела «Учётные данные»
5. После входа откройте «Каталог агентов» или «Входящая корреспонденция»
   (после логина открывается дашборд /, не /agents автоматически)

--- Примечания ---
- Vite уже слушает 0.0.0.0:{{FRONTEND_PORT}} — флаг --host не нужен.
- API проксируется через Vite на {{API_HOST}} — клиент должен достучаться до сервера.
- Переопределить логин/пароль: PLATFORM_LOGIN_EMAIL, PLATFORM_LOGIN_PASSWORD в .env

============================================================

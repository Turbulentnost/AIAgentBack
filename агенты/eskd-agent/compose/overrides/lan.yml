# LAN access: extra frontend port for Windows portproxy (8000).
services:
  frontend:
    ports:
      - "${BIND_ADDRESS:-0.0.0.0}:${FRONTEND_PORT:-3000}:80"
      - "${BIND_ADDRESS:-0.0.0.0}:${FRONTEND_LAN_PORT:-8000}:80"

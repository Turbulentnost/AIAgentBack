# Debug backend: verbose logs, integration worker off, postgres exposed.
services:
  postgres:
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:${POSTGRES_PORT:-5432}:5432"

  backend:
    environment:
      LOG_LEVEL: debug
      INTEGRATION_WORKER_ENABLED: "false"
